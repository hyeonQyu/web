<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.nextwin.forum.mappers.board">

	<select id="list" resultType="org.nextwin.forum.dto.BoardDto">
		select bno, title, content, writer, regDate, viewCount from board
	</select>
	
	<insert id="write" parameterType="org.nextwin.forum.dto.BoardDto">
		insert into board(title, content, writer) values(#{title}, #{content}, #{writer})
	</insert>

</mapper>